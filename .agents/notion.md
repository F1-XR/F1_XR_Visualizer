# Notion Rules

Read this file only when the task requires a Notion document.

- Find and read the document's `AI 작업 지침` before summarizing, planning, or changing anything.
- Start the response by naming the `AI 작업 지침` that was checked.
- Follow those document-specific instructions.
- If no `AI 작업 지침` is found, state that before continuing.
